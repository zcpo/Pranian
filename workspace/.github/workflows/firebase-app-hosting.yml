
# This file was auto-generated by Firebase Studio.
#
# To connect this repository to Firebase App Hosting, make sure you
# have the following secrets configured in your GitHub repository settings:
#
# - FIREBASE_SERVICE_ACCOUNT_KEY: A JSON service account key with the "Firebase App Hosting Admin" role.
# - PROJECT_ID: The ID of your Firebase project.
#
# For more information, see the Firebase App Hosting documentation.

name: Deploy to Firebase App Hosting
'on':
  push:
    branches:
      - main
permissions:
  contents: read
  pull-requests: write
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 'Authenticate to Google Cloud'
        uses: 'google-github-actions/auth@v2'
        with:
          credentials_json: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_KEY }}'
      - name: 'Deploy to App Hosting'
        uses: 'firebase/firebase-apphosting-deploy@v0'
        with:
          projectId: '${{ secrets.PROJECT_ID }}'
