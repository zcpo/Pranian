{
  "rules": {
    ".read": false,
    ".write": false,
    "feed_items": {
      ".read": true,
      ".indexOn": ["createdAt"],
      "$postId": {
        ".write": "auth != null",
        "userId": {
          ".validate": "newData.val() === auth.uid"
        },
         "createdAt": {
           ".validate": "newData.val() <= now"
         }
      }
    },
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
