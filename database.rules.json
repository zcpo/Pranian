{
  "rules": {
    ".read": false,
    ".write": false,
    "feed_items": {
      ".read": "auth != null",
      ".write": "auth != null",
      ".indexOn": ["createdAt"],
      "$postId": {
        "userId": {
          ".validate": "newData.val() === auth.uid"
        },
         "createdAt": {
           ".validate": "newData.val() <= now"
         }
      }
    },
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
